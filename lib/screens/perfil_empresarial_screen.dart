import 'package:flutter/material.dart';

class PerfilEmpresarialScreen extends StatelessWidget {
  const PerfilEmpresarialScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Perfil Empresarial')),
      body: const Center(child: Text('Pantalla de perfil empresarial')),
    );
  }
}
